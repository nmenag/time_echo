<%= t('mailers.future_letter.written_on', date: I18n.l(@letter.created_at.to_date, format: :long)) %>

<%= t('mailers.future_letter.header') %>
=============================

<%= truncate(@letter.content, length: 160, separator: ' ', omission: '…') %>

<%= t('mailers.future_letter.view_full_prompt') %>
<%= t('mailers.future_letter.open_btn') %>:
<%= Rails.application.routes.url_helpers.letter_url(@letter.signed_id, host: ENV.fetch('APP_HOST') { 'localhost:3000' }) %>

--
<%= t('mailers.future_letter.footer_text') %>
