<!DOCTYPE html>
<html lang="<%= I18n.locale %>" data-theme="<%= current_user_theme %>" class="scroll-smooth">
  <head>
    <title><%= content_for(:title) || "TimeEcho — #{t('slogan')}" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="TimeEcho">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="description" content="<%= t('landing.subtitle') %>">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "flatpickr", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-base-100 text-base-content font-sans min-h-screen flex flex-col antialiased selection:bg-secondary">
    <!-- Active Layout Switcher -->
    <% 
      current_route = "#{controller_path}/#{action_name}"
      no_sidebar_pages = %w[pages/landing sessions/new check_emails/show letter_successes/show]
      show_sidebar = user_signed_in? && !no_sidebar_pages.include?(current_route)
    %>

    <% if show_sidebar %>
      <!-- Desktop & Mobile Responsive Sidebar Layout -->
      <div class="flex flex-col md:flex-row min-h-screen w-full">
        <!-- Sidebar Navigation (Desktop only, sticky) -->
        <aside class="hidden md:flex w-72 h-screen sticky top-0 bg-base-200/50 border-r border-base-200 p-8 flex-col justify-between shrink-0 font-sans">
          <div class="space-y-10">
          <!-- Brand Logo / Sparkle -->
          <div class="flex items-center justify-between">
            <a href="<%= root_path %>" class="flex items-center gap-2 text-2xl font-serif italic text-primary tracking-tight font-normal group">
              <span class="text-brand transition-transform group-hover:rotate-12 duration-300">✨</span>
              <%= t('brand') %>
            </a>

            <%= button_to locales_path(locale: toggle_locale), method: :post, class: "flex items-center gap-1.5 text-base-content/60 hover:text-brand transition-colors cursor-pointer bg-transparent border-none p-0", data: { turbo: "false" } do %>
              <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12a9 9 0 1 0 18 0 9 9 0 0 0-18 0z" />
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12h18M12 3v18" />
              </svg>
              <span class="text-xs font-bold"><%= I18n.locale == :en ? "EN" : "ES" %></span>
            <% end %>
          </div>

            <!-- Navigation Links -->
            <nav class="space-y-2">
              <!-- Write Letter -->
              <% write_active = current_page?(new_letter_path) %>
              <a href="<%= new_letter_path %>" class="flex items-center gap-3.5 px-4 py-3 rounded-lg text-sm font-medium transition-colors duration-150 <%= write_active ? 'bg-primary text-primary-content' : 'text-base-content/70 hover:bg-base-200 hover:text-primary' %>">
                <span class="text-base" aria-hidden="true">✏️</span>
                <span><%= t('navigation.write') %></span>
              </a>

              <!-- My Letters -->
              <% vault_active = current_page?(dashboard_path) && !params[:timeline] %>
              <a href="<%= dashboard_path %>" class="flex items-center gap-3.5 px-4 py-3 rounded-lg text-sm font-medium transition-colors duration-150 <%= vault_active ? 'bg-primary text-primary-content' : 'text-base-content/70 hover:bg-base-200 hover:text-primary' %>">
                <span class="text-base" aria-hidden="true">✉️</span>
                <span><%= t('navigation.vault') %></span>
              </a>

              <!-- Timeline -->
              <% timeline_active = current_page?(dashboard_path) && params[:timeline] %>
              <a href="<%= dashboard_path(timeline: true) %>" class="flex items-center gap-3.5 px-4 py-3 rounded-lg text-sm font-medium transition-colors duration-150 <%= timeline_active ? 'bg-primary text-primary-content' : 'text-base-content/70 hover:bg-base-200 hover:text-primary' %>">
                <span class="text-base" aria-hidden="true">🕒</span>
                <span><%= t('navigation.timeline') %></span>
              </a>

              <!-- Analytics -->
              <% analytics_active = current_page?(analytics_path) %>
              <a href="<%= analytics_path %>" class="flex items-center gap-3.5 px-4 py-3 rounded-xl text-sm font-medium transition-colors duration-150 <%= analytics_active ? 'bg-primary text-primary-content' : 'text-base-content/70 hover:bg-base-200 hover:text-primary' %>">
                <span class="text-base" aria-hidden="true">📊</span>
                <span><%= t('navigation.analytics') %></span>
              </a>

              <!-- Settings -->
              <% settings_active = current_page?(settings_path) %>
              <a href="<%= settings_path %>" class="flex items-center gap-3.5 px-4 py-3 rounded-xl text-sm font-medium transition-colors duration-150 <%= settings_active ? 'bg-primary text-primary-content' : 'text-base-content/70 hover:bg-base-200 hover:text-primary' %>">
                <span class="text-base" aria-hidden="true">⚙️</span>
                <span><%= t('navigation.settings') %></span>
              </a>
            </nav>
          </div>

          <!-- Bottom Quote & User Card -->
          <div class="space-y-6">
            <!-- Inspirational Quote -->
            <div class="border border-base-200 bg-base-200/40 p-4 rounded-xl space-y-2 select-none">
              <p class="text-xs font-serif italic text-base-content/70 leading-relaxed font-normal">
                 "<%= t('navigation.quote') %>"
              </p>
            </div>

            <!-- Active User -->
            <% if user_signed_in? %>
              <div class="flex items-center justify-between border-t border-base-200 pt-4">
                <div class="min-w-0 flex-1">
                  <p class="text-xs text-base-content/50 font-medium truncate pr-2"><%= current_user_email %></p>
                </div>
                <%= button_to logout_path, method: :delete, class: "btn btn-ghost btn-circle btn-sm text-error hover:bg-error/10 cursor-pointer flex items-center justify-center transition-transform duration-200 hover:scale-105 active:scale-95", data: { turbo: "false" }, "aria-label": t('navigation.sign_out') do %>
                  <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" class="w-4 h-4">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M15.75 9V5.25A2.25 2.25 0 0013.5 3h-6a2.25 2.25 0 00-2.25 2.25v13.5A2.25 2.25 0 007.5 21h6a2.25 2.25 0 002.25-2.25V15m3 0l3-3m0 0l-3-3m3 3H9" />
                  </svg>
                <% end %>
              </div>
            <% else %>
              <a href="<%= login_path %>" class="btn btn-primary text-primary-content btn-sm rounded-xl w-full text-xs font-semibold border-none cursor-pointer hover:scale-[1.02] active:scale-[0.98] transition-transform duration-200">
                <%= t('navigation.sign_in') %>
              </a>
            <% end %>
          </div>
        </aside>

        <!-- Mobile Top Navbar -->
        <header class="md:hidden sticky top-0 z-50 bg-base-100/90 backdrop-blur-md border-b border-base-200 px-4 py-3 flex items-center justify-between">
          <a href="<%= root_path %>" class="flex items-center gap-1.5 text-xl font-serif italic text-primary tracking-tight font-normal">
            <span class="text-brand" aria-hidden="true">✨</span>
            <%= t('brand') %>
          </a>

          <div class="flex items-center gap-2">
            <%= button_to locales_path(locale: toggle_locale), method: :post, class: "flex items-center gap-1 text-base-content/60 hover:text-brand transition-colors cursor-pointer bg-transparent border-none p-0 hover:scale-[1.02] active:scale-[0.98]", data: { turbo: "false" } do %>
              <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12a9 9 0 1 0 18 0 9 9 0 0 0-18 0z" />
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12h18M12 3v18" />
              </svg>
              <span class="text-xs"><%= I18n.locale == :en ? "EN" : "ES" %></span>
            <% end %>

            <!-- Mobile Menu Dropdown -->
            <details class="dropdown dropdown-end dropdown-bottom">
              <summary class="btn btn-ghost btn-circle btn-sm list-none cursor-pointer">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h7" /></svg>
              </summary>
              <ul class="menu dropdown-content bg-base-100 border border-base-200 rounded-box w-52 p-2 shadow-md gap-1 z-50">
                <li><a href="<%= new_letter_path %>" class="py-2">✏️ <%= t('navigation.write') %></a></li>
                <li><a href="<%= dashboard_path %>" class="py-2">✉️ <%= t('navigation.vault') %></a></li>
                <li><a href="<%= dashboard_path(timeline: true) %>" class="py-2">🕒 <%= t('navigation.timeline') %></a></li>
                <li><a href="<%= analytics_path %>" class="py-2">📊 <%= t('navigation.analytics') %></a></li>
                <li><a href="<%= settings_path %>" class="py-2">⚙️ <%= t('navigation.settings') %></a></li>
                <% if user_signed_in? %>
                  <div class="divider my-1"></div>
                  <li class="px-3 py-1.5 text-[10px] text-base-content/40 truncate"><%= current_user_email %></li>
                  <li><%= button_to t('navigation.sign_out'), logout_path, method: :delete, class: "text-error py-2 text-left w-full cursor-pointer", data: { turbo: "false" } %></li>
                <% else %>
                  <li><a href="<%= login_path %>" class="py-2 font-semibold text-primary"><%= t('navigation.sign_in') %></a></li>
                <% end %>
              </ul>
            </details>
          </div>
        </header>

        <!-- Right Content Area -->
        <main class="flex-grow min-h-screen bg-base-100 p-4 sm:p-8 md:p-12 overflow-x-hidden font-sans">
          <div id="flash-container">
            <% if flash[:notice].present? %>
              <div class="bg-emerald-50 border border-emerald-200 rounded-2xl shadow-xs flex items-center gap-3.5 py-3.5 px-5 mb-6 animate-fade-in max-w-5xl mx-auto text-emerald-950 w-full">
                <span class="text-base shrink-0">✨</span>
                <span class="text-xs font-semibold leading-relaxed"><%= flash[:notice] %> ✨</span>
              </div>
            <% end %>
            <% if flash[:alert].present? %>
              <div class="bg-rose-50 border border-rose-200 rounded-2xl shadow-xs flex items-center gap-3.5 py-3.5 px-5 mb-6 animate-fade-in max-w-5xl mx-auto text-rose-950 w-full">
                <span class="text-base shrink-0">⚠️</span>
                <span class="text-xs font-semibold leading-relaxed"><%= flash[:alert] %></span>
              </div>
            <% end %>
          </div>

          <%= yield %>
        </main>
      </div>
    <% else %>
      <!-- Clean Full-Width Layout (for Landing, Login, Success pages) -->
      <header class="sticky top-0 z-50 bg-base-100/90 backdrop-blur-md border-b border-base-200/60">
        <div class="max-w-6xl mx-auto px-4 md:px-6 h-16 flex items-center justify-between">
          <a href="<%= root_path %>" class="flex items-center gap-0.5 text-2xl font-serif italic text-base-content tracking-tight font-normal">
            <span>TimeEcho</span>
            <span class="text-brand text-xl font-sans relative -top-2.5 font-bold" aria-hidden="true">+</span>
          </a>

          <div class="flex items-center gap-8 text-sm font-medium">
            <%= button_to locales_path(locale: toggle_locale), method: :post, class: "flex items-center gap-1.5 text-base-content/60 hover:text-brand transition-colors cursor-pointer bg-transparent border-none p-0 hover:scale-[1.02] active:scale-[0.98]", data: { turbo: "false" } do %>
              <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12a9 9 0 1 0 18 0 9 9 0 0 0-18 0z" />
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12h18M12 3v18" />
              </svg>
              <%= I18n.locale == :en ? "EN" : "ES" %>
            <% end %>

            <div class="h-6 w-px bg-base-300"></div>

            <a href="<%= root_path %>#how-it-works" class="text-base-content/60 hover:text-brand transition-colors"><%= t('landing.how_it_works') %></a>

            <% if user_signed_in? %>
              <a href="<%= dashboard_path %>" class="btn text-white rounded-xl px-5 text-xs font-semibold border-none cursor-pointer hover:scale-[1.02] active:scale-[0.98] transition-transform duration-200" style="background-color: oklch(40% 0.22 290); --btn-color: oklch(40% 0.22 290);"><%= t('navigation.vault') %></a>
            <% else %>
               <a href="<%= login_path %>" class="btn text-white rounded-xl px-5 text-sm font-semibold border-none cursor-pointer hover:scale-[1.02] active:scale-[0.98] transition-transform duration-200" style="background-color: oklch(40% 0.22 290); --btn-color: oklch(40% 0.22 290);"><%= t('navigation.sign_in') %></a>
            <% end %>
          </div>
        </div>
      </header>


      <main class="flex-grow max-w-6xl mx-auto px-4 md:px-6 py-10 w-full font-sans">
        <!-- Flash Banners -->
        <% if flash[:notice].present? %>
          <div class="bg-emerald-50 border border-emerald-200 rounded-2xl shadow-xs flex items-center gap-3.5 py-3.5 px-5 mb-6 animate-fade-in text-emerald-950 w-full">
            <span class="text-base shrink-0">✨</span>
            <span class="text-xs font-semibold leading-relaxed"><%= flash[:notice] %> ✨</span>
          </div>
        <% end %>
        <% if flash[:alert].present? %>
          <div class="bg-rose-50 border border-rose-200 rounded-2xl shadow-xs flex items-center gap-3.5 py-3.5 px-5 mb-6 animate-fade-in text-rose-950 w-full">
            <span class="text-base shrink-0">⚠️</span>
            <span class="text-xs font-semibold leading-relaxed"><%= flash[:alert] %></span>
          </div>
        <% end %>

        <%= yield %>
      </main>

      <!-- Beautiful Minimalist Footer -->
      <footer class="footer footer-center p-8 bg-base-200/50 border-t border-base-200 text-base-content/60 font-sans">
        <div class="max-w-6xl mx-auto flex flex-col sm:flex-row items-center justify-between w-full gap-4 text-xs">
          <aside class="flex items-center gap-2">
            <span>&copy; <%= Time.current.year %> TimeEcho. <%= t('footer_copy') %></span>
          </aside>
          <nav class="flex gap-4 items-center">
            <a href="<%= about_path %>" class="hover:text-primary transition-colors"><%= t('pages.footer.about') %></a>
            <a href="<%= privacy_path %>" class="hover:text-primary transition-colors"><%= t('pages.footer.privacy') %></a>
            <a href="<%= terms_path %>" class="hover:text-primary transition-colors"><%= t('pages.footer.terms') %></a>
            <span class="font-serif italic text-base-content/40"><%= t('slogan') %></span>
          </nav>
        </div>
      </footer>
    <% end %>

    <% if user_signed_in? %>
      <!-- Reusable Premium Settings Modal -->
      <dialog id="settings_modal" class="modal modal-bottom sm:modal-middle font-sans">
        <div class="modal-box bg-base-100 border border-base-200 rounded-box p-8 space-y-6">
            <h3 class="font-serif italic text-2xl text-primary font-normal flex items-center gap-2">
              <span>⚙️</span> <%= t('navigation.settings') %>
            </h3>
          <p class="text-xs text-base-content/50 leading-relaxed">
            <%= t('settings.subtitle') %>
          </p>

          <div class="divider my-1"></div>

          <div class="space-y-4">
            <div class="flex items-center justify-between bg-base-200/30 p-3 rounded-xl border border-base-200/50">
              <div>
                <p class="text-xs font-semibold text-primary"><%= t('settings.notifications_title') %></p>
                <p class="text-[10px] text-base-content/40"><%= t('settings.notifications_desc') %></p>
              </div>
              <input type="checkbox" class="toggle toggle-primary toggle-sm" checked />
            </div>

            <div class="flex items-center justify-between bg-base-200/30 p-3 rounded-xl border border-base-200/50">
              <div>
                <p class="text-xs font-semibold text-primary"><%= t('settings.global_private_mode_title') %></p>
                <p class="text-[10px] text-base-content/40"><%= t('settings.global_private_mode_desc') %></p>
              </div>
              <input type="checkbox" class="toggle toggle-primary toggle-sm" checked />
            </div>
          </div>

          <div class="modal-action">
            <form method="dialog" class="w-full flex gap-3">
              <button class="btn btn-outline border-base-300 hover:bg-base-200 rounded-xl text-sm font-semibold flex-1 cursor-pointer"><%= t('common.cancel') %></button>
              <button class="btn btn-primary rounded-xl text-sm font-semibold flex-1 cursor-pointer"><%= t('common.save') %></button>
            </form>
          </div>
        </div>
      </dialog>
    <% end %>
  </body>
</html>
