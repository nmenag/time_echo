Written on <%= @letter.created_at.strftime('%B %d, %Y') %>

A Message from Your Past Self
=============================

<%= @letter.content %>

To view this letter in your secure vault, visit:
<%= Rails.application.routes.url_helpers.letter_url(@letter.signed_id, host: ENV.fetch('APP_HOST') { 'localhost:3000' }) %>

--
You received this because you wrote a letter to your future self using TimeEcho.
"Sent with hope, received with nostalgia."
